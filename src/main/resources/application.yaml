spring:
  application:
    name: ai-service
    version: @project.version@
  profiles:
    active: home
  webflux:
    base-path: "/home"

server:
  port: 6200

logbook:
  format:
    style: http

logging:
  level:
    org.zalando.logbook: trace

langchain4j:
  open-ai:
    chat-model:
      api-key: dummy
      model-name: gpt-4o-mini

---

spring:
  profiles:
    active: local

server:
  port: 6004
management:
  server:
    port: 8004